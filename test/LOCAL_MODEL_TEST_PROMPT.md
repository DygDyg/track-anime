# Track Anime — Тест локальной модели

Ты — AI-ассистент, которого тестируют на знание проекта Track Anime.
Прочитай документацию проекта и ответь на все задания ниже.

**Формат ответа:** создай файлы с результатами в папке `test/results/`:

1. `test/results/01-architecture.md` — ответы на раздел 1
2. `test/results/02-invariants.md` — ответы на раздел 2
3. `test/results/03-business-logic.md` — ответы на раздел 3
4. `test/results/04-code-navigation.md` — ответы на раздел 4
5. `test/results/05-bug-hunt.md` — ответы на раздел 5
6. `test/results/06-refactoring.md` — ответы на раздел 6
7. `test/results/07-conventions.md` — ответы на раздел 7
8. `test/results/08-docs-impact.md` — ответы на раздел 8
9. `test/results/09-code-generation.md` — ответы на раздел 9 (сюда же код)
10. `test/results/10-edge-cases.md` — ответы на раздел 10

Перед каждым ответом пиши номер вопроса. Если не знаешь — пиши `НЕДОСТАТОЧНО ДАННЫХ`. Обязательно подкрепляй ответы ссылками на реальные файлы и строки.

---

## Раздел 1 — Архитектура (10 вопросов)

1.1. Опиши стек проекта: фреймворк, БД, ORM, внешние API, авторизация. Укажи конкретные файлы конфигурации.

1.2. Нарисуй (текстом) схему данных: как связаны `KodikMaterial`, `Shikimori anime`, `UserWatchProgress`, `AnimeExternalIdMap`. Укажи ключевые поля.

1.3. Объясни, как данные попадают из Kodik API в базу данных. Назови файлы и функции в цепочке import/sync.

1.4. Где находится серверная логика, а где клиентская? Назови правило и 3 примера файлов для каждой стороны.

1.5. Как устроен App Router в этом проекте? Перечисли все dynamic route segments и их назначение.

1.6. Объясни механизм кэширования в проекте: какие API кэшируются, как, с какими тегами? Где это настроено?

1.7. Как устроена система нотификаций? Какие каналы поддерживаются? Назови ключевые файлы.

1.8. Что такое brand rotation в контексте проекта? Какие файлы отвечают за эту функцию?

1.9. Как работает система cover/poster proxy? Опиши цепочку fallback. Назови файлы.

1.10. Объясни архитектуру watch party: клиент, сервер, протокол, роли участников. Какие файлы задействованы?

---

## Раздел 2 — Инварианты проекта (8 вопросов)

2.1. Какой главный доменный ключ проекта? Запиши равенство из трёх частей.

2.2. Почему браузерный код НЕ может вызывать Shikimori/Kodik API напрямую? Где это проверяется?

2.3. Что уникально идентифицирует watch progress пользователя? Почему НЕ по translation?

2.4. Как проверяется авторизация в API routes? Назови функции и покажи пример использования в реальном файле.

2.5. На каком языке user-facing строки? Приведи 3 примера из реального кода.

2.6. Что такое `KodikMaterial` с точки зрения домена? Чем он отличается от «аниме»?

2.7. Где хранится состояние долгих операций (import/sync)? Назови Prisma-модели.

2.8. Может ли один `shikimoriId` иметь несколько `KodikMaterial`? Объясни почему и приведи пример.

---

## Раздел 3 — Бизнес-логика (10 вопросов)

3.1. Опиши flow главной ленты (home feed): от данных в БД до рендера на клиенте. Назови все функции и компоненты.

3.2. Как работает фильтр «завершённые тайтлы» в ленте? Где порог в 90 дней?

3.3. Опиши flow страницы аниме: что загружается, в каком порядке, какие компоненты рендерят что.

3.4. Как TA-плеер определяет, когда переключить на следующую серию? Почему «примерно за секунду до конца»?

3.5. Как работает автопропуск OP/ED? Откуда берутся тайминги? Что такое intro-offset?

3.6. Опиши механизм совместного просмотра (watch party): создание комнаты, подключение по ключу, синхронизация.

3.7. Как работает поиск? Какой движок используется? Укажи файл с SQL.

3.8. Опиши flow OAuth-авторизации через Shikimori. Какие файлы участвуют?

3.9. Как работает QR-логин? Назови файлы и опиши flow.

3.10. Что происходит при sync с Kodik? Как обнаруживаются новые серии? Где создаётся `KodikEpisodeRelease`?

---

## Раздел 4 — Навигация по коду (10 вопросов)

Для каждого вопроса: найди реальный файл, укажи путь и номера строк.

4.1. Где определена функция `getRecentReleasesPage`? Какие параметры она принимает?

4.2. Где обрабатывается OAuth callback от Shikimori?

4.3. Где находится Prisma schema? Перечисли 5 ключевых моделей и их назначение.

4.4. Найди файл, который отвечает за проксирование обложек. Какой route обрабатывает запрос?

4.5. Где определены типы Kodik API? Перечисли ключевые интерфейсы.

4.6. Найди компонент TA-плеера. Какие хуки он использует?

4.7. Где находится логика сохранения watch progress? Проследи цепочку от клиента до БД.

4.8. Найди middleware проекта. Что он делает?

4.9. Где определена функция `requireAdmin`? В каких файлах она вызывается (найди минимум 3)?

4.10. Найди конфиг Next.js. Какие rewrites/redirects настроены?

---

## Раздел 5 — Поиск багов (5 вопросов)

Прочитай код и найди потенциальные проблемы. Для каждого: файл, строки, описание проблемы, предложение фикса.

5.1. Проверь `src/lib/search.ts` — есть ли риск SQL injection? Как параметры попадают в запрос?

5.2. Проверь `src/lib/releases.ts` — корректно ли работает cursor pagination? Может ли возникнуть бесконечный цикл или дубли?

5.3. Проверь `src/lib/poster.ts` — что произойдёт, если все источники обложек недоступны? Есть ли fallback на дефолтное изображение?

5.4. Проверь `src/app/api/search/route.ts` — есть ли rate limiting или защита от abuse?

5.5. Проверь `src/db/save-material.ts` — что произойдёт при race condition (два одновременных upsert одного материала)?

---

## Раздел 6 — Рефакторинг (5 вопросов)

Предложи минимальный рефакторинг. Для каждого: текущий код (цитата), проблема, предложенный код.

6.1. Найди дублирование логики между `src/lib/search.ts` и `src/lib/search-shared.ts`. Предложи, как устранить.

6.2. Найди в проекте место, где одна функция делает слишком много (>100 строк). Предложи декомпозицию.

6.3. Найди magic numbers или hardcoded strings, которые стоило бы вынести в константы. Минимум 3 примера.

6.4. Найди место, где error handling можно улучшить (молчаливый catch, отсутствие логирования). Предложи исправление.

6.5. Есть ли в проекте неиспользуемые exports или dead code? Найди минимум 2 примера.

---

## Раздел 7 — Конвенции проекта (5 вопросов)

7.1. Какой стиль именования файлов используется? (kebab-case, camelCase, PascalCase — для каких типов файлов?)

7.2. Как организованы импорты? Есть ли path aliases? Какие?

7.3. Какой паттерн используется для API routes? (App Router conventions)

7.4. Как в проекте разделены серверные и клиентские компоненты? Есть ли `"use client"` / `"use server"` директивы?

7.5. Прочитай `CONVENTIONS.md` и перечисли 5 ключевых правил, которые должен соблюдать разработчик.

---

## Раздел 8 — Документация (5 вопросов)

8.1. Если я добавлю новый API route `/api/user/bookmarks` — какие документы нужно обновить? Перечисли конкретные файлы и секции.

8.2. Если я изменю формат ответа `/api/releases` — что сломается? Какие компоненты зависят от этого API?

8.3. Если я добавлю новый канал нотификаций (email) — какие файлы и документы затронуты?

8.4. Если я поменяю Prisma schema (добавлю поле в `KodikMaterial`) — какие шаги нужно выполнить? Какие файлы обновить?

8.5. Составь чеклист документации для PR, который добавляет новую страницу `/settings` с пользовательскими настройками.

---

## Раздел 9 — Генерация кода (5 заданий)

Напиши реальный работающий код, соблюдая конвенции проекта.

9.1. Напиши API route `src/app/api/user/bookmarks/route.ts`, который возвращает список закладок пользователя (используя существующие паттерны auth и Prisma).

9.2. Напиши серверную функцию `getAnimeByGenre(genre: string, limit: number)` в стиле существующих функций в `src/lib/`, которая ищет аниме по жанру в БД.

9.3. Напиши React компонент `GenreBadge` в стиле существующих компонентов проекта — бейдж с названием жанра, который ведёт в поиск.

9.4. Напиши middleware-функцию rate limiter для API routes, совместимую с существующей архитектурой.

9.5. Напиши Prisma migration для добавления таблицы `UserBookmark` с полями: `id`, `userId`, `shikimoriId`, `note`, `createdAt`. Используй стиль существующей schema.

---

## Раздел 10 — Edge Cases и сценарии (5 вопросов)

10.1. Что произойдёт, если пользователь откроет аниме, которого нет в БД, но есть на Shikimori? Проследи весь flow.

10.2. Что произойдёт при одновременном просмотре одного аниме в двух вкладках? Как сохранится прогресс?

10.3. Что произойдёт, если Kodik API вернёт ошибку во время sync? Есть ли retry? Где это обрабатывается?

10.4. Что произойдёт, если сессия пользователя истекла во время просмотра? Потеряется ли прогресс?

10.5. Что произойдёт, если мастер watch party отключится? Как передаётся роль?

---

## Инструкция по оценке

После генерации результатов попроси оценщика (основную модель) прочитать файлы из `test/results/` и оценить по критериям:

| Критерий | Вес |
|----------|-----|
| Корректность ответов (совпадение с реальным кодом) | 30% |
| Полнота (все вопросы отвечены) | 20% |
| Конкретность (файлы, строки, функции — не абстракции) | 20% |
| Соблюдение конвенций в сгенерированном коде | 15% |
| Качество найденных багов и рефакторинга | 15% |

Итоговый файл оценки: `test/results/EVALUATION.md`
